# MoveChat 1.2

本版本引入了与 multiChat 保持一致的更新能力与版本说明体验。

- 应用启动后会自动检查远端版本信息，并在检测到新版本时弹出更新提示。
- “关于 MoveChat”面板支持手动检查更新、查看当前版本说明，以及直接跳转下载页。
- 更新说明采用 `manifest.json + releases/<version>/<locale>.md` 的远端结构，维护方式与来源项目一致。
- 当前项目的 UI 弹窗、状态文案和版本比较逻辑与来源实现保持一致。
